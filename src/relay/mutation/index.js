import { createUser, editUserById } from './user';
import { createPatient, editPatientById } from './patient';
import { createEventType, editEventTypeById } from './eventType';
import { createEvent, editEventById } from './event';
import login from './login';

export default {
  createUser,
  editUserById,
  createPatient,
  editPatientById,
  createEventType,
  editEventTypeById,
  createEvent,
  editEventById,
  login
};
